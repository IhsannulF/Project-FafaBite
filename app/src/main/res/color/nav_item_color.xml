<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Warna saat menu sedang dipilih/aktif (Biru FafaBite) -->
    <item android:color="@color/fafa_blue_primary" android:state_checked="true" />

    <!-- Warna saat menu tidak dipilih (Abu-abu) -->
    <item android:color="@color/text_grey" />
</selector>